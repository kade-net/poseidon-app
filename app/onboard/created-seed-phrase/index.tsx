import { View, Text } from 'react-native'
import React from 'react'
import CreatedSeedPhrase from '../../../screens/onboarding/created-seed-phrase'

const CreatedSeedPhraseScreen = () => {
    return (
        <CreatedSeedPhrase />
    )
}

export default CreatedSeedPhraseScreen